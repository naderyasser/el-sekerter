<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- الاتصال بالسيرفر عشان يفهم الكلام -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- الإدخال بالصوت -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>

    <!-- سكرتير حقيقي: يقرا التقويم ويكتب فيه، ويعرف مين أبو سعد، ويطلبه -->
    <uses-permission android:name="android.permission.READ_CALENDAR"/>
    <uses-permission android:name="android.permission.WRITE_CALENDAR"/>
    <uses-permission android:name="android.permission.READ_CONTACTS"/>
    <!-- CALL_PHONE يطلب الرقم مباشرة. من غيره يفتح شاشة الاتصال والمستخدم
         يضغط — التطبيق يشتغل بالحالتين. -->
    <uses-permission android:name="android.permission.CALL_PHONE"/>

    <!-- التذكيرات -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <!-- بدون RECEIVE_BOOT_COMPLETED كل التذكيرات المجدولة تضيع لما الجهاز
         يعيد التشغيل — المستقبِل تحت هو اللي يعيد جدولتها. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- الجدولة الدقيقة: SCHEDULE_EXACT_ALARM يطلب إذن من المستخدم على
         أندرويد 13، و USE_EXACT_ALARM ممنوح تلقائيًا من أندرويد 14 لتطبيقات
         التنبيه والتقويم — وهذا التطبيق منها. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
    <!-- التذكير يبان زي منبّه حقيقي حتى والشاشة مقفولة. -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>
    <!-- استثناء من توفير البطارية: أجهزة كتير (شاومي/أوبو/ريلمي خصوصًا)
         بتقتل المنبّهات المجدولة لتوفير الطاقة — فالصفّارة ما ترنّش رغم إن
         كل الأذونات متاحة. الإذن ده بيطلّع حوار «اسمح» للمستخدم. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>

    <application
        android:label="السكرتير"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- مستقبِلات flutter_local_notifications. الثالث هو اللي يعيد جدولة
             التذكيرات بعد إعادة تشغيل الجهاز أو تحديث التطبيق. -->
        <receiver android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ActionBroadcastReceiver"/>
        <receiver android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"/>
        <receiver android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- بدون هذا، speech_to_text ما يلقى محرّك التعرّف على الكلام على
             أندرويد 11 فما فوق. -->
        <intent>
            <action android:name="android.speech.RecognitionService"/>
        </intent>
        <!-- عشان canLaunchUrl تعرف إذا واتساب منصّب ولا لأ، ولفتح
             الاتصال والرسايل من أندرويد 11 فما فوق. -->
        <package android:name="com.whatsapp"/>
        <package android:name="com.whatsapp.w4b"/>
        <!-- سكيم whatsapp:// المباشر — أضمن من wa.me اللي ممكن يروح للمتصفح. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="whatsapp"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.DIAL"/>
            <data android:scheme="tel"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.SENDTO"/>
            <data android:scheme="smsto"/>
        </intent>
    </queries>
</manifest>
